# Plugin Naming Convention

## Plugin Name Standard

All plugins in LobeHub Editor should follow a consistent naming convention for the `pluginName` static property.

### Standard Format

```typescript
static pluginName = 'PluginNamePlugin';
```

**Rules:**

- Use PascalCase (first letter capitalized)
- Always end with "Plugin" suffix
- Be descriptive and clear about functionality

### Examples

```typescript
// ✅ Correct naming
static pluginName = 'MathPlugin';
static pluginName = 'FilePlugin';
static pluginName = 'ImagePlugin';
static pluginName = 'CodeblockPlugin';
static pluginName = 'MarkdownPlugin';
static pluginName = 'UploadPlugin';
static pluginName = 'SlashPlugin';

// ❌ Incorrect naming
static pluginName = 'math';         // Missing Plugin suffix, lowercase
static pluginName = 'upload';       // Missing Plugin suffix, lowercase
static pluginName = 'image';        // Missing Plugin suffix, lowercase
static pluginName = 'slash';        // Missing Plugin suffix, lowercase
```

### Complete Plugin Name List

| Plugin    | Correct pluginName |
| --------- | ------------------ |
| Math      | `MathPlugin`       |
| File      | `FilePlugin`       |
| Image     | `ImagePlugin`      |
| Upload    | `UploadPlugin`     |
| Slash     | `SlashPlugin`      |
| Mention   | `MentionPlugin`    |
| HR        | `HRPlugin`         |
| Common    | `CommonPlugin`     |
| Codeblock | `CodeblockPlugin`  |
| Code      | `CodePlugin`       |
| List      | `ListPlugin`       |
| Markdown  | `MarkdownPlugin`   |
| Table     | `TablePlugin`      |
| Link      | `LinkPlugin`       |

### Implementation Pattern

```typescript
export const PluginClass: IEditorPluginConstructor<PluginOptions> = class
  extends KernelPlugin
  implements IEditorPlugin<PluginOptions>
{
  static pluginName = 'PluginNamePlugin'; // Always follow this pattern
  
  // Rest of plugin implementation...
};
```

### Benefits

1. **Consistency**: All plugins follow the same naming pattern
2. **Clarity**: Easy to identify plugin names in logs and debugging
3. **Professional**: Maintains consistent codebase standards
4. **Searchability**: Easy to search for plugin-related code

### Debug Logging

Plugin names appear in debug logs with this format:

```
lobe-editor:kernel 🔌 Plugin: PluginNamePlugin
```

This standardization ensures clean, consistent debug output across all plugins.
