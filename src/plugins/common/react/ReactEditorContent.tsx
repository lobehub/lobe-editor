'use client';

import type { FC } from 'react';

import type { ReactEditorContentProps } from './type';

const ReactEditorContent: FC<ReactEditorContentProps> = () => {
  return null;
};

ReactEditorContent.displayName = 'ReactEditorContent';

export default ReactEditorContent;
